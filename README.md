# HAKODASE / ハコダセ

倉庫の色分け出荷をテーマにした、ブラウザ向けの短時間タイムアタック型パズルです。
色のついた箱（コンテナ）をドラッグして、同じ色の搬出口へ運びます。すべて運び終えるとクリア。クリアタイムを競います。

- ビルド不要・外部依存ゼロの静的サイト（HTML / CSS / バニラ JS）
- スマートフォン（iPhone SE 相当・横幅 320px）優先
- Canvas2D 描画（将来 Three.js/WebGL に差し替え可能な設計）
- seed 付きランダム生成。ランキング対象は「最短20手以上」を保証

---

## 起動方法

依存パッケージのインストールは不要です。Node.js があればそのまま動きます。

```
npm run dev
```

起動したら、表示される URL（既定では http://localhost:5173/ ）をブラウザで開いてください。
※ `index.html` をファイルとして直接開くと ES Modules の制約で動きません。必ず上記サーバ経由で開いてください。

ポートを変えたいとき:

```
PORT=8080 npm run dev
```

## テスト方法

Node 標準のテストランナーだけで動きます（追加インストール不要）。

```
npm test
```

RNG の再現性、移動判定、一方通行床、開閉ゲート、ソルバーの最短手数、生成盤面の20手保証、ランキング保存などを検証します。

## 遊び方

1. 箱をドラッグした方向へ 1 マス動かします（離すとスナップします）。
2. すべての箱を同じ色（記号）の搬出口へ入れるとクリアです。
3. タイマーは最初の操作で始まり、クリアで止まります。
4. 「やりなおし」で同じ盤面を最初から、「新しい盤面」で別の盤面を出します。
5. seed を指定すると同じ盤面を再挑戦できます（公平なタイム比較に便利）。

### ギミック

- **矢印床（一方通行）**: その床にいる箱は、矢印の向きにしか出られません。
- **シャッター（開閉ゲート）**: 手数に応じて開閉します。閉じている間は通れません。
- **色と記号**: 色だけでなく ●▲■◆★＋ の記号も併記しているので、色の見分けが苦手でも遊べます。

### 難易度

| 難易度 | 色数 | ランキング | 特徴 |
| --- | --- | --- | --- |
| 練習 | 2 | × | 壁・矢印床ありの小盤面で操作に慣れる |
| 初級 | 3 | × | ゲートも登場 |
| 標準 | 4 | ○ | 最短20手以上を保証 |
| 上級 | 5 | ○ | ゲート増 |
| 達人 | 6 | ○ | 6色の色分け出荷 |

ランキング対象（標準以上）の盤面は、最短でも20手以上かかることを生成時に保証しています。

## ディレクトリ構成

```
index.html              エントリ
styles/main.css         スタイル
src/
  main.js               統合・ゲームループ
  core/                 ゲームロジック（描画非依存・Nodeでテスト可能）
    rng.js  rules.js  solver.js  generator.js  engine.js  palette.js
  render/               描画
    renderer.js         Renderer インターフェース
    canvas-renderer.js  Canvas2D 実装
  input/pointer-input.js  Pointer Events 入力
  ui/hud.js             HUD の DOM 更新
  services/ranking.js   ランキング（localStorage / 将来オンライン化可能）
scripts/dev-server.mjs  依存なし静的サーバ
test/*.test.js          Node 標準テスト
docs/                   要件・実装計画・アーキテクチャ
```

## 設計の要点

- **ゲームロジックは描画に依存しません。** `core/` は Node だけで import・テストできます。
- 描画は `frameState`（読み取り専用スナップショット）を受け取って描くだけ。`Renderer` インターフェースを実装すれば `CanvasRenderer` を `ThreeRenderer` に差し替えられます。
- ランキングは `RankingService` として分離。将来オンライン版へ差し替え可能です。
- 「最短20手」は論理上の 1 マス移動を 1 手として数えます。ランキング盤面はマンハッタン距離の下界で20手以上を保証し、ソルバーで最短手数を確認します。

詳細は `docs/requirements.md` / `docs/implementation-plan.md` / `docs/architecture.md` を参照してください。

## 今後の拡張

- Three.js/WebGL 版レンダラー（`Renderer` を実装して差し替え）
- 1×2 / 2×1 などの複数マスブロック
- オンラインランキング（`RankingService` の差し替え）
- 生成・探索の Web Worker 化
- Web Audio による効果音

## オリジナリティ

本作は「色ブロックを整理する短時間パズル」という抽象的な発想から独自に作ったオリジナル作品です。
名称・画面構成・ステージ・演出・素材・文章は既存ゲームから流用していません。画像・音声などの外部素材は一切使わず、すべて Canvas で描画しています。

## ライセンス

MIT
